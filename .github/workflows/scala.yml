name: Scala CI

on:
  push:
    branches: [ ctyun ]
  pull_request:
    branches: [ ctyun ]

jobs:
  build:

    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v2
    - name: Set up JDK 1.8
      uses: actions/setup-java@v1
      with:
        java-version: 1.8
    - name: Run tests
      run: mvn -N install
    - name: install
      run: mvn clean install
